# Frontend Adversarial Review — Findings & Triage

Reviewer: Opus 5 sub-agent (session-inherited medium effort), read-only, ran the dev stack in Chromium. Findings verbatim-condensed; triage verdicts by Fable orchestrator; fixes land in PR 5 (`fix(review): FE-<n>` commits).

| # | Sev | Finding | Verdict |
|---|---|---|---|
| FE-1 | **critical** | **QR encodes `http://localhost:3001/...` (API origin), not the client origin.** Vite proxy shorthand rewrites the Host header to the target, so `req.get('host')` is the API host; the code comment claiming otherwise is factually wrong. Port 3001 serves no client routes, so the scanned link is dead — functionally the hardcoded-localhost failure the plan called out. | **Accept.** Fix: vite proxy object form with `changeOrigin: false` so the browser's Host passes through; in prod (single origin, server serves static) the host is inherently right. Verify with a LAN-host header test. |
| FE-2 | major | Format dropdown race: stale `/api/formats` response can overwrite a newer game's list (no cleanup/sequence guard); enables submitting an MTG format under a Yu-Gi-Oh! selection (server only checks the format exists). | **Accept.** Fix: effect cleanup with a staleness flag. (Server-side game/format cross-check is FE-5's shared-rule territory; the format still must exist, which server enforces.) |
| FE-3 | major | RegisterPage hangs on bare "Loading…" forever on fetch failure/unknown event; no error branch, no testid. | **Accept.** Fix: not-found/error state + `register-not-found` testid, mirroring EventPage. |
| FE-4 | major | `api()` assumes JSON responses: an HTML 500 surfaces as a raw `SyntaxError` parser message under a non-contract `register-error` testid; also server `VALIDATION` responses have no `message`, so the literal string "VALIDATION" renders. | **Accept.** Fix: guard `res.json()`, map unknown errors to a friendly message + `register-error` documented as the generic fallback id; include first Zod issue in server validation `message`. (Server half waits for PR 4 window.) |
| FE-5 | major | The one non-trivial capacity rule (`capacity ≥ format.minPlayers`) is hand-rolled twice (client + server) instead of living in shared code; client copy silently no-ops when the format list is stale. | **Accept.** Fix: shared `capacityRule(minPlayers)` (schema refinement factory) used by both sides; single message source. Touches `shared/` → sequenced after backend panel completes. |
| FE-6 | major | Register validation discards Zod issues; 61-char name renders misleading "Please enter your name." | **Accept.** Fix: surface the Zod issue message; add `maxLength` to the input. |
| FE-7 | minor | `register-error-full` unreachable on an already-full event: submit is disabled, so the graded "navigate to full event → submit → see full error" path shows only a disabled button. | **Accept.** Fix: keep submit enabled; let the server answer EVENT_FULL and render `register-error-full` (the full-state text remains as a hint). |
| FE-8 | minor | Calendar/Create pages swallow fetch failures (`console.error`); backend-down renders as an innocent empty month. | **Accept (lite).** Fix: minimal shared error banner with testid on both pages. |
| FE-9 | minor | EventPage conflates network failure with "not found"; `notFound`/`event` never reset on `id` change (stale/stuck states latent). | **Accept (lite).** Fix: reset state in effect; distinguish 404 (`event-not-found`) from other failures (`event-error`). |
| FE-10 | minor | Event-page spots-left is a mount-time snapshot, not live; full events still show a primary Register button. | **Reject (scope)** for liveness — polling/SSE is out of timebox, refresh-on-mount is acceptable for the brief; noted in README cut list. **Accept** the cheap half: hide/disable the Register button when full. |
| FE-11 | minor | Duration presentation: "depending on attendance" only in hover tooltip; `min==max` renders self-contradicting "runs 3h depending on attendance"; `toFixed(1)` misrounds (3.25 h → "3.3h"). | **Accept.** Fix: `h:mm`-style formatting (`3h15`), "runs ~3h" phrasing when min==max, full note text assertable on the event page (calendar cell keeps the compact form for space). |
| FE-12 | minor | Calendar fetches all events once, ignores `from`/`to` and never refetches on month navigation. | **Reject (scope).** Fine at store scale; documented as a deliberate cut in the README. |
| FE-13 | minor | testid gaps: loading states, header home link, FullCalendar toolbar, no catch-all 404 route. | **Accept (lite).** Fix: `event-loading`/`register-loading` + header link id + catch-all route with testid. FullCalendar's own toolbar buttons stay library-classed (e2e can use `.fc-next-button`). |

**Clean categories (verified by reviewer):** timezone conversion of `datetime-local` → UTC (two timezones, exact), XSS via player names (escaped, no `dangerouslySetInnerHTML` anywhere), Fluent v9 Select usage, calendar click-through, client-visible last-seat race (distinct success/full rendering across two tabs).

**Meta-note for the AI-usage log:** FE-1 disproves a factual claim I (Fable) made and encoded in a code comment ("the Vite proxy forwards the client's Host header") *and* covered with a passing test — the test asserted the echo of a Host header that real proxied traffic never carries. Genuine catch that a same-model review might have rationalized away.
